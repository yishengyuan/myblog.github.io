# 钱包\合约交互

## 钱包交互(非合约的操作)

---

**Navbar**

### **Web3Provider & Signer**

### 判断、切换链

---

`wallet_switchEthereumChain`

### 连接metamask

---

`eth_requestAccounts`

*通过请求钱包账号来触发钱包登陆（初次打开浏览器进入页面需要登录钱包）*

### 选择账号？

---

metamask不直接提供切换账号的接口

可以在页面上提示用户切换账号来做相关交互

我们可以监听账号改变来做一些切换账户的逻辑变更的操作 

`window.ethereum.**on**('accountsChanged', function(accounts){})`

### 往metamask添加新网络？

---

EIP-3085

`wallet_addEthereumChain`

```jsx
{
	chainId,
	chainName,
	rpcUrls,
	nativeCurrency,
}
```