### 流动性减少时的计算

1. **流动性减少的计算**
   - 当流动性从 l0 减少到 L1 时，我们需要移除 DX 数量的 token X 和 DY 数量的 token Y。这个公式适用于不同的池值函数定义，无论我们将 PO 值函数定义为 `x * y`，还是 `2x` 或 `2y`，该公式都成立。
2. **池值函数的定义**
   - 如果我们将池值函数定义为 `2x` 或 `2y`，则计算方法比较直接，学员可以自行练习这一部分内容。
   - 在接下来的课程中，我们将讨论当池值函数定义为 `sqrt(x * y)` 时，如何推导出该公式。